import { HomeBox, HomeTitle } from './Home.styled';

const Home = () => {
  return (
    <HomeBox>
      <HomeTitle>This is an application for storing your contacts</HomeTitle>
    </HomeBox>
  );
};

export default Home;
