import { Login } from 'components/Login/Login';
import { AuthBox } from './Authorization.styled';

const Authorization = () => {
  return (
    <AuthBox>
      <Login />
    </AuthBox>
  );
};

export default Authorization;
